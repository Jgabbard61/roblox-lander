# Database URL (PostgreSQL)
# Required for all API operations
DATABASE_URL="postgresql://user:password@host:port/database"

# Redis URL (for rate limiting and caching)
# Required for API rate limiting
# If not provided, falls back to localhost:6379
REDIS_URL="redis://localhost:6379"

# Stripe Configuration (Phase 4 - not yet implemented)
# Required for credit purchases
STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Stripe Webhook Secret (Phase 4 - not yet implemented)
# Used to verify webhook signatures
STRIPE_WEBHOOK_SECRET="whsec_..."

# Main App URL (roblox-tool deployment)
# This is where the registration API endpoint is hosted
NEXT_PUBLIC_APP_URL="https://www.verifylens.com"

# Lander URL (this site)
NEXT_PUBLIC_SITE_URL="https://site.verifylens.com"

# Node Environment
NODE_ENV="development"
